# URL base da API Flask. Se o backend rodar na porta padrão (5000), pode
# nem precisar deste arquivo — apiClient.ts já usa http://localhost:5000
# como fallback. Descomente/ajuste se o backend rodar em outra porta/host.
# VITE_API_URL=http://localhost:5000
